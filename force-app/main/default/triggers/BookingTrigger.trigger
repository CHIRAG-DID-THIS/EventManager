trigger BookingTrigger on Booking__c (after insert) {
    /*
    for (Booking__c booking : Trigger.new) {
        if (booking.Id != null) {
            mailingclass.sendBookingMail(booking.Id);
        }
    }
*/
}